
public class MultiplePlaces {

       public def distributeMultiplePlaces(){
       /*
       public def distributeMultiplePlaces(mr:MapReduce[M,R],data:Array[M],
						inNumAsyncs:Int,inNumPlaces:Int){R haszero}:R 
       
       */
       
       /*
       val length = data.region.max(0) + 1;
       var numAsyncs:Int = inNumAsyncs;
       var numPlaces:Int = inNumPlaces;
       
       if(length < numAsyncs){
       	numAsyncs = length;
       }else if(length < numPlaces){
        numPlaces = 1;
       }
       
       val results = new Array[R](numPlaces);       

       */

       }


       private def doParallelChunkMultiplePlace(){
       /*
       val length = data.region.max(0) + 1;
       val inputsPerAsync = length / numAsyncs;
       val start = id * inputsPerAsync;
       val end = (id == (numAsyncs - 1)) ? length - 1 : (start + inputsPerAsync - 1);
       */



       }


       public static def main(argv:Array[String]]){




       }






}